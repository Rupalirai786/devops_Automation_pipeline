name: automationscript
on:
 push:
  branches:
  - main

jobs:
 build-job:
  runs-on: ubuntu:latest
  steps:
   - name: cloning
     run: git clone https://github.com/Rupalirai786/devops_Automation_pipeline
   - name: generating image from dockerfile
     run: docker build -t img .
	
  
